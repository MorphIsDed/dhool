export default function Zone0Entry({ onEnter }) {
  return (
    <section className="zone" id="Zone0Entry">
      <div style={{ padding: '4rem', color: 'var(--ui-cream)' }}>
        <h2>Zone0Entry — Coming Soon</h2>
      </div>
    </section>
  )
}
