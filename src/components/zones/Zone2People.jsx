export default function Zone2People({ onEnter }) {
  return (
    <section className="zone" id="Zone2People">
      <div style={{ padding: '4rem', color: 'var(--ui-cream)' }}>
        <h2>Zone2People — Coming Soon</h2>
      </div>
    </section>
  )
}
